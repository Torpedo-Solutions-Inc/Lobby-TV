{
  "name": "lobby-television",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n server,frontend -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "render-build": "npm ci --prefix frontend && npm ci --prefix server && npm run build"
  }
}

